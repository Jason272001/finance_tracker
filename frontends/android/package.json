{
  "name": "keeperbma-android",
  "version": "1.0.0",
  "private": true,
  "description": "KeeperBMA Android companion app",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:copy": "npx cap copy android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.3.0",
    "@capacitor/core": "^7.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.3.0"
  }
}